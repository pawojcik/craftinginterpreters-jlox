class Point {
    init(x, y) {
        this.x = x;
        this.y = y;
    }

    move(dx, dy) {
        return Point(this.x + dx, this.y + dy);
    }

    str() {
        print this.x;
        print this.y;
    }
}

var p = Point(1, -3);
p.str();
p.move(3, 4).str();